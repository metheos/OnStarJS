{
  "name": "Codespace Environment",
  "postCreateCommand": "sudo apt update && sudo DEBIAN_FRONTEND=noninteractive apt upgrade -y && sudo apt install xvfb -y && corepack enable && if [ \"$CODESPACES\" = \"true\" ]; then echo '' | corepack install; else corepack install; fi && sudo pnpm exec patchright install-deps && pnpm install"
}
