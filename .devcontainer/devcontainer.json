{
  "name": "Codespace Environment",
  "postCreateCommand": "sudo apt update && sudo DEBIAN_FRONTEND=noninteractive apt upgrade -y && sudo apt install xvfb -y && corepack enable && yes | corepack install && sudo pnpm exec patchright install-deps && pnpm install"
}
